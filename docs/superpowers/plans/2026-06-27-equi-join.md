# Equi-Join Primitive Implementation Plan (DM-8)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`) syntax.

**Goal:** `hash_join(left_key_id, right_key_id)` — inner hash equi-join over two uint32 key columns → matching `(left_row, right_row)` index pairs. Verified against a brute O(n·m) oracle.

**Spec:** `docs/superpowers/specs/2026-06-27-equi-join-design.md` (contains the full `hash_join` code).

---

### Task 1: hash_join + test

**Files:** Modify `compute_mock.cpp`; Create `test_join.cpp`.

- [ ] **Step 1: Write the failing test** — Create `test_join.cpp`:

```cpp
// CPU test for the equi-join primitive (DM-8): hash_join(left,right) returns matching (left_row,
// right_row) pairs; verified against a brute O(n*m) oracle, incl. duplicates + a COLD column.
#include "compute_mock.cpp"
#include <cassert>
#include <cstdint>
#include <algorithm>
#include <utility>
#include <vector>
#include <iostream>

using Pairs = std::vector<std::pair<uint64_t, uint64_t>>;
static Pairs sorted(Pairs v) { std::sort(v.begin(), v.end()); return v; }
// Brute reference: every (i,j) with l[i]==r[j].
static Pairs brute(const std::vector<uint32_t>& l, const std::vector<uint32_t>& r) {
    Pairs out;
    for (uint64_t i = 0; i < l.size(); ++i) for (uint64_t j = 0; j < r.size(); ++j)
        if (l[i] == r[j]) out.emplace_back(i, j);
    return sorted(out);
}

static void test_join_basic() {
    std::vector<uint32_t> L = {10, 20, 30, 20}, R = {20, 40, 10, 20};
    CPUMockEngine eng;
    eng.load_scan_column(1, L.data(), L.size());
    eng.load_scan_column(2, R.data(), R.size());
    Pairs got = sorted(eng.hash_join(1, 2));
    Pairs exp = brute(L, R);
    assert(got == exp && "hash join == brute oracle");
    // explicit expected set: (0,2),(1,0),(1,3),(3,0),(3,3)
    assert((got == Pairs{{0,2},{1,0},{1,3},{3,0},{3,3}}) && "exact pairs incl. duplicate-key Cartesian");
    std::cout << "[join basic] ok\n";
}

static void test_join_edges() {
    CPUMockEngine eng;
    std::vector<uint32_t> a = {1, 2, 3}, b = {4, 5, 6};         // disjoint
    eng.load_scan_column(1, a.data(), a.size());
    eng.load_scan_column(2, b.data(), b.size());
    assert(eng.hash_join(1, 2).empty() && "no matches -> empty");
    std::vector<uint32_t> c = {7, 7}, d = {7};                 // dup left, single right
    eng.load_scan_column(3, c.data(), c.size());
    eng.load_scan_column(4, d.data(), d.size());
    assert((sorted(eng.hash_join(3, 4)) == Pairs{{0,0},{1,0}}) && "Cartesian of dup match");
    std::cout << "[join edges] ok\n";
}

static void test_join_cold() {
    // Tiny host budget (bytes): L(16B)+R(8B)=24 > 16, so the idle column demotes to COLD; the join
    // borrows it back. Scanning L keeps it hot so R is the eviction victim.
    CPUMockEngine eng(0, "", 16);
    std::vector<uint32_t> L = {5, 9, 5, 1}, R = {9, 5};        // 16B, 8B
    eng.load_scan_column(1, L.data(), L.size());
    eng.load_scan_column(2, R.data(), R.size());
    for (int i = 0; i < 8; ++i) {                              // heat col1 -> rebalance demotes idle col2
        MatrixQuery q{}; q.value_col = 1; q.agg = AGG_COUNT; std::vector<uint64_t> o; eng.execute_query(q, o);
    }
    assert(eng.column_tier(2) == MemorySpace::COLD && "idle join column demoted -> exercises the borrow path");
    Pairs got = sorted(eng.hash_join(1, 2));
    assert(got == brute(L, R) && "join correct across the COLD tier (borrow)");
    assert((got == Pairs{{0,1},{1,0},{2,1}}) && "L{5,9,5,1} x R{9,5}: 5@0=5@1, 9@1=9@0, 5@2=5@1");
    std::cout << "[join cold] ok\n";
}

int main() { test_join_basic(); test_join_edges(); test_join_cold();
    std::cout << "ALL JOIN TESTS PASSED\n"; return 0; }
```

- [ ] **Step 2: Run to verify it fails** — `cd /Users/saikrishna.2177481/Documents/Spike/MatrixDB && clang++ -std=c++20 -O2 test_join.cpp -o /tmp/tj && /tmp/tj` → FAIL to compile (`hash_join` undeclared).

- [ ] **Step 3: Add hash_join** — In `compute_mock.cpp`, add the `hash_join` method (spec §2 verbatim) as a PUBLIC method near `grouped_aggregate` / `execute_query` (it borrows columns like they do).

- [ ] **Step 4: Run to verify it passes** — `clang++ -std=c++20 -O2 -Wall -Wextra test_join.cpp -o /tmp/tj && /tmp/tj` → PASS: `[join basic] ok`, `[join edges] ok`, `[join cold] ok`, `ALL JOIN TESTS PASSED`. Zero warnings.

- [ ] **Step 5: Confirm no regression** — additive (one new method); these MUST still pass unmodified:
  - `for t in test_live_tiering test_schema test_query test_typed_columns test_group_by test_observability test_backup; do clang++ -std=c++20 -O2 $t.cpp -o /tmp/$t 2>/dev/null && /tmp/$t | tail -1; done`
  - `clang++ -std=c++20 -O3 -mcpu=apple-m1 main.cpp -o /tmp/mdb && /tmp/mdb 2>&1 | grep "Scan result sum"` → `83886070 (oracle 83886070)`.
  If any differ, STOP / report BLOCKED.

- [ ] **Step 6: Commit**

```bash
cd /Users/saikrishna.2177481/Documents/Spike/MatrixDB
git add compute_mock.cpp test_join.cpp
git -c user.name=garikipatisai-code -c user.email=garikipatisai-code@users.noreply.github.com commit -m "feat: equi-join primitive (DM-8) — hash_join over two uint32 key columns -> matching row pairs"
```

---

### Task 2: Regression + notebook

**Files:** Modify `make_notebook.py`; Regenerate `matrixdb_colab.ipynb`.

- [ ] **Step 1: Full CPU suite (36 tests).** Run `./run_tests.sh` — expect `ALL GREEN (36 tests + oracle)`, exit 0. If any fail, STOP / report BLOCKED.

- [ ] **Step 2: Notebook** — add `"test_join.cpp"` to `make_notebook.py` SOURCES right after `"test_query_parser.cpp"`; add a run cell after the query-parser run cell (`test_query_parser.cpp` → `/tmp/tqparse`):
```python
    md("### Equi-join primitive\n"
       "hash_join(left, right) inner-joins two uint32 key columns into matching (left_row, right_row) "
       "pairs (build-hash + probe) — multi-table correlation; verified against a brute-force oracle."),
    code("!clang++ -std=c++20 -O2 test_join.cpp -o /tmp/tj 2>/dev/null "
         "|| g++ -std=c++20 -O2 test_join.cpp -o /tmp/tj; /tmp/tj"),
```
Then `python3 make_notebook.py` → expect `wrote matrixdb_colab.ipynb: <N> cells, 55 source files embedded`. Verify `grep -o "test_join.cpp" matrixdb_colab.ipynb | wc -l` → `>= 2`.

- [ ] **Step 3: Commit**

```bash
cd /Users/saikrishna.2177481/Documents/Spike/MatrixDB
git add make_notebook.py matrixdb_colab.ipynb
git -c user.name=garikipatisai-code -c user.email=garikipatisai-code@users.noreply.github.com commit -m "chore: embed equi-join test in Colab notebook"
```

---

## Self-Review
**Spec coverage:** hash_join (§2)→T1S3; match+duplicates (Cartesian) + no-match + COLD-tier + brute-oracle non-vacuity (§3)→T1S1; regression + oracle (§3)→T1S5; suite+notebook→T2. ✓
**Placeholders:** none — `hash_join` verbatim in spec §2. **Type consistency:** `hash_join(left_key_id, right_key_id)→vector<pair<uint64_t,uint64_t>>`; asserts both `column_type==U32`; borrow-and-return mirrors `grouped_aggregate`. Brute O(n·m) oracle in the test is independent of the SUT. Additive; oracle/id-0 untouched.
